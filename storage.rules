rules_version = '2';
// Receitas assinadas: só o servidor grava e gera links temporários.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} { allow read, write: if false; }
  }
}
